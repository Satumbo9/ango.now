import React from "react";
import { menuLinks } from "../data/menuLinks";
import Link from "next/link";

const NavMenu = () => {
  return (
    <section className="h-full flex  flex-col gap-10 justify-center items-center bg-sky-500">
      <p>Angola now</p>
    </section>
  );
};

export default NavMenu;
